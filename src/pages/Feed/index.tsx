import { Container } from '@mui/material';

const Home = () => {
  const a = 0;
  const b = 1;
  const c = a + b;
  return <Container sx={{ py: 2 }}>HOME</Container>; // py === padding-top + padding-bottom
};

export default Home;
